export interface Task {
    taskId?: number;      // Represents the ID of the task
    name: string;        // Represents the name of the task
    description: string; // Represents the description of the task
  }
  